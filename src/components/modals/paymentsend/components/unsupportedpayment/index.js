import { connect, Component, bindActionCreators, Fragment } from 'component';

// Third Party Imports ...


import './index.scss';

const mapStateToProps = (state, props) => {
  return ({});
};

const mapDispatchToProps = (dispatch, props) => {
  return ({});
};

class components_modals_paymentsend_components_unsupportedpayment extends Component {




  render() {
    return (
      <div className="components_modals_paymentsend_components_unsupportedpayment">
        <p>components_modals_paymentsend_components_unsupportedpayment</p>
      </div>
    );
  }
}

export default connect(mapStateToProps, mapDispatchToProps)(components_modals_paymentsend_components_unsupportedpayment);


